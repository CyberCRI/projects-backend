query PopulateFromCrisalid($limit: Int, $offset: Int) {
  documents(limit: $limit, offset: $offset) {
    uid,
    publication_date,
    document_type

    titles {
      language
      value
    }

    has_contributions {
      roles,
      contributor {
        uid
        display_name,
        names {
          first_names {
              value
              language
          }
          last_names {
            value
            language
          }
        }
        identifiers {
          type
          value
        }
      }
    }

    recorded_by {
      harvester
      uid,
    }
  }
}